# Procedimiento de ejecución del desafio 8

## Configuración

1. Primero se debe crear un archivo en la raiz del proyecto, con el nombre 'env', que debe tener el siguiente contenido:

```
NODE_PORT=3000
```

Con esa variable de entorno se está definiendo el puerto que usará el servidor.

## Ejecutar

2. En segundo lugar, se deben ejecutar los siguientes comandos